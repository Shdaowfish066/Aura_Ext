# The Gemini API key lives ONLY in server/.env (GEMINI_API_KEY).
# It must never appear in a VITE_* variable — Vite inlines those into the
# public extension bundle.

# Optional: FastAPI backend base URL (defaults to http://127.0.0.1:8000)
VITE_AURA_API_URL=http://127.0.0.1:8000
