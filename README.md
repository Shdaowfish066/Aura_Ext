# 🌐 Aura — The AI Browser Companion

> *Your browser finally knows you.*

Aura is a Chrome Extension that replaces your new tab page with an AI-powered
personal dashboard. It learns your interests, reads your mood, curates your
world, and actually cares about your wellbeing.

Built with the Google Gemini API. Built for humans.

---

## ✨ Features

### 🏠 Smart New Tab Dashboard
Your new tab is no longer blank. It's your world — a beautiful, dark-themed
dashboard with your curated feed, AI search, stats, and more. Loads instantly.

### 🧠 Taste Engine
Aura silently learns what you love by tracking which sites you visit and how
long you stay. No form to fill out. No stars to rate. It just learns.

### 😶‍🌫️ Mood Detection
A quick mood check-in every session. You pick how you feel — Aura reshuffles
your entire feed to match. Tired? Lighter content. Hyped? Deep reads.

### 📰 Unified AI Feed
One feed. All your interests. Anime news, tech articles, sports headlines —
pulled from multiple sources, deduplicated, and curated by Gemini with a
personal "why you'd like this" caption on every card.

### 🔍 AI-Powered Search
Type anything in the Aura search bar. Gemini answers directly — not a list of
links. Ask for recommendations, get answers, find things faster.

### 🕐 Wellness System
Aura tracks how long you've been browsing and nudges you to rest. Not
annoyingly — smartly. Gemini generates a fresh, never-repeated micro-activity
every time you need a break.

### 🎮 Mini Games
Hit your break threshold and a 2-minute mini game launches — trivia questions
generated by Gemini based on *your* actual browsing history. Always relevant.
Never generic.

---

## 🚀 Getting Started

### Prerequisites

- Node.js 18+
- Chrome or any Chromium-based browser (Brave, Edge, Arc)
- A [Google Gemini API key](https://aistudio.google.com/apikey) (free tier works)
  — goes in `server/.env`, never in the extension
- (Optional) Python 3.11+ for the FastAPI backend in `server/`

### Installation

```bash
# 1. Clone the repo
git clone https://github.com/your-username/aura-extension
cd aura-extension

# 2. Install dependencies
npm install

# 3. Set up environment variables
cp .env.example .env.local
# Add your API keys to .env.local

# 4. Build the extension
npm run build

# 5. Load into Chrome
# → Go to chrome://extensions
# → Enable "Developer Mode" (top right toggle)
# → Click "Load unpacked"
# → Select the dist/ folder
```

### Environment Variables

The Gemini API key lives **only** in `server/.env` (see `server/README.md`) so
it never ships inside the extension bundle:

```env
# server/.env
GEMINI_API_KEY=your_gemini_api_key
```

The optional client-side `.env.local` only configures the backend URL:

```env
VITE_AURA_API_URL=http://127.0.0.1:8000
```

---

## 🏗️ Tech Stack

| Layer | Technology |
|-------|-----------|
| Extension | Chrome Extensions Manifest V3 |
| Frontend | React 18 + Vite |
| Styling | Tailwind CSS |
| AI Layer | Google Gemini API (gemini-2.5-flash) |
| Storage | chrome.storage.local |
| News | NewsAPI |
| Anime Data | Jikan API (free, no key needed) |
| Tech News | HackerNews API (free, no key needed) |

---

## 📁 Project Structure

```
aura-extension/
├── manifest.json           # Chrome extension config
├── src/
│   ├── background/         # Service worker (session tracking, nudges)
│   ├── content/            # Page tracker (injected into sites)
│   ├── newtab/             # Main dashboard UI (React)
│   │   ├── components/
│   │   │   ├── Feed/       # Feed panel + cards
│   │   │   ├── Search/     # AI search bar + results
│   │   │   ├── Mood/       # Mood widget + badge
│   │   │   ├── Stats/      # Session stats panel
│   │   │   ├── Wellness/   # Nudge toasts + break overlay
│   │   │   ├── Games/      # Trivia game + launcher
│   │   │   └── Onboarding/ # First-time setup wizard
│   │   └── hooks/          # useFeed, useSearch, useMood, etc.
│   ├── claude/             # API client + all prompt templates
│   └── storage/            # Typed chrome.storage wrapper
└── public/icons/           # Extension icons
```

---

## 🤖 How Gemini Powers Aura

Aura uses Gemini at 4 key moments:

| Moment | What Gemini Does |
|--------|-----------------|
| Feed loads | Ranks articles + writes personal captions for each card |
| User searches | Answers directly with taste-aware recommendations |
| Break time | Generates a fresh, personalized wellness micro-activity |
| Mini game | Creates trivia questions from your actual browsing history |

---

## 🎨 Design

Aura uses a dark glass-morphism aesthetic:

- **Base:** Deep navy (`#0d0f1a`)
- **Accent:** Electric indigo (`#6366f1`) + warm amber (`#f59e0b`)
- **Fonts:** Clash Display (headings) + Satoshi (body)
- **Motion:** Smooth 300ms transitions, staggered feed animations, liquid
  mood reshuffles

---

## 🔒 Privacy

Aura is **fully local-first.**

- All your browsing data stays on your device in `chrome.storage.local`
- No account required, no backend, no database
- The only external calls are:
  - Google Gemini API (to generate captions, answers, trivia, break prompts),
    proxied through your own local FastAPI server
  - Dev.to / Jikan / HackerNews (to fetch article content)
- Your taste profile and history are **never sent anywhere** except as
  context to Gemini for curation

---

## 🗺️ Roadmap

- [x] Core dashboard + feed
- [x] Mood detection + feed reshuffle
- [x] AI search
- [x] Session tracking + wellness nudges
- [x] Claude-generated trivia mini game
- [ ] Passive mood detection via webcam (opt-in, face-api.js)
- [ ] Multiple game types (word scramble, quick reflex)
- [ ] End-of-day summary notification
- [ ] Feed source customization panel
- [ ] Export your taste profile
- [ ] Firefox support

---

## 🤝 Contributing

This project was built for a hackathon. Contributions, issues, and feature
requests are welcome after the initial release.

---

## 📄 License

MIT — do whatever you want with it.

---

*Built with Gemini. Built for you.*
